{
  "name": "threshold-studio-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/sync-assets.mjs && node scripts/build-pages.mjs && node scripts/build-indexes.mjs",
    "test": "node --test website/tests/*.test.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^18.0.10"
  }
}
